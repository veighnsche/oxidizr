name: mdBook

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  book-build:
    name: mdBook build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: peaceiris/actions-mdbook@v2
        with:
          mdbook-version: 'latest'
      - name: Build book
        run: mdbook build cargo/switchyard/book
      - name: Upload book artifact (preview)
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: mdbook-site
          path: cargo/switchyard/book/book
          if-no-files-found: error

  book-deploy:
    name: mdBook deploy (Pages)
    runs-on: ubuntu-latest
    needs: book-build
    if: github.ref == 'refs/heads/main'
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
      - uses: peaceiris/actions-mdbook@v2
        with:
          mdbook-version: 'latest'
      - name: Build book
        run: mdbook build cargo/switchyard/book
      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: cargo/switchyard/book/book
          force_orphan: true
